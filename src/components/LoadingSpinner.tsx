const LoadingSpinner = () => (
  <div
    style={{ borderTopColor: 'transparent' }}
    className="self-center mx-auto w-16 h-16 border-4 border-teal-300 border-solid rounded-full animate-spin"
  />
);

export default LoadingSpinner;
